extends layout

include mixins/_songRow

block content
  .row
    .mt-3.mb-3
      h1=title
    table.table
      thead
        tr
          th(scope="col") Artist
          th(scope="col") Title
          th(scope="col")
        tbody
          each song in songs
            +songRow(song)

      
      